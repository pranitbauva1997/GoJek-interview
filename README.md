## Introduction

The project files are in the `root` directory. The source code is in the `src` directory.



### Dependencies

This project is compatible with Python2 and Python3

### Testing

From the `root` directory, execute this code:

```
./bin/parking_lot file_inputs.txt && python src/t01.py && cd src/ && ./test-parser.sh && cd ..
```



### Coverage

Checkout the file [htmlcov/index.html](htmlcov/index.html)